# Derived, disposable knowledge index — rebuilt by scripts/kb-index.ts.
# Everything else under .promptus/ (the stores) is committed; only the cache is derived.
/.promptus/cache/

# Node / bun
node_modules/
*.log

# Warp terminal agent file — local-only, not part of the repo
WARP.md
