% GENERATED by editio-render from sections/introduction.md — edits here are disposable.
% class: deo:Introduction · status: drafting

\section{Introduction}\label{sec:introduction}
\editiostamp{2026-07-03}{editio-paper-read-port, the-gate}

Long-running research assistants forget. \claimV{A file-based store with a gated writer keeps what a project knows retrievable across sessions}\editiogrounds{the-gate}, and \claimC{the same discipline appears to transfer to manuscripts}\editiogrounds{editio-paper-read-port}. Prior work established the information-theoretic framing \cite{shannon1948,vaswani2017}, and we reported early results in \selfcite{ourpaper}.

\claimU{Roughly 40\% of retracted papers fail on provenance, not analysis}. \claimG{Editors increasingly ask for claim-level evidence trails}.

The method is defined in \cref{sec:methods}, where the entropy $H = -\sum_i p_i \log p_i$ is \textbf{bounded} by construction; the \emph{derivation} uses the \texttt{kb-find} primitive. We evaluate on 50\% of the corpus \& report \#tags\_used per section.

Our contributions:

\begin{itemize}
  \item A structure gate grounded in DoCO and DEO
  \item Three renders from one markdown source
  \item An audit loop that grades every claim against a store
\end{itemize}
\blindhide{This work was funded by Grant 12-345 at Affiliation One.}

\begin{equation}\label{eq:entropy}
  H(X) = -\sum_{i=1}^{n} p_i \log p_i
\end{equation}

The bound in \cref{eq:entropy} holds for any finite alphabet.

\claimV{The gate refuses off-vocab writes even under load (\cref{sec:methods}), as prior work anticipated \cite{shannon1948}}\editiogrounds{the-gate}.

The frozen benchmark mean is \editionum{bakeoff-mean-ari} (the drift was $\Delta = \editionum{bakeoff-mean-ari} - 0.866$), and \claimV{the fixed configuration leads at \editionum{bakeoff-mean-ari}}\editiogrounds{the-gate}.

\begin{table}[tb]
  \caption{Grounding beats retrieval alone \cite{vaswani2017}; mean \editionum{bakeoff-mean-ari}; details in \cref{sec:methods}.}
  \label{tab:grounding}
\end{table}

\subsection{Scope of the contract}

\begin{enumerate}
  \item The subset stays small
  \item The fixture stays byte-exact
\end{enumerate}
Symbols survive prose: \textasciitilde{}40 runs, x\textasciicircum{}2, a\textbackslash{}b path, \{braces\} — and the table in \cref{tab:grounding} is crossref-able like "\cref{sec:methods}" in quotes.

\[J(\theta) = \sum_i \|x_i\|^2\]

\claimC{Ablations confirm the trend}.

\begin{verbatim}
int x = 1; // fence tags may carry symbols
\end{verbatim}

\subsubsection{Coda}

Done.
